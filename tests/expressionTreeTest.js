const assert = require('assert');

describe('ExpressionTree', () => {
  describe('#evaluate()', () => {
    it('should return the evaluated expression', () => {
      //[
      //  '5',
      //  '5+5',
      //  '10/2',
      //  '2.2*3',
      //  '(2)',
      //  '(2.2*3)+5/6',
      //  '(2.2*3)+(5*3)+6'
      //].forEach((str) => {
      //    assert.equal(eval(str), new ExpressionTree(str).evaluate());
      //  });
    });
  });
});
